coverage:
  precision: 2
  round: down
  range: "50...100"
  status:
    project:
      default:
        target: 60%
        threshold: 20%
    patch:
      default:
        target: 60%
        whreshold: 20%

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: no
      macro: no

comment:
  layout: "reach,diff,flags,files,footer"
  behavior: default
  require_changes: false

ignore:
  - "tests/"
  - "**/*.md"
  - "**/*.yml"
  - "**/*.yaml"
  - "**/*.toml"
